# sfsEdgeStore 测试方案实施文档

---

## 文档信息

| 项目 | 内容 |
|------|------|
| **文档版本** | v2.0 |
| **创建日期** | 2026-05-11 |
| **适用版本** | sfsEdgeStore v1.0+ |
| **测试环境** | Windows/Linux/macOS |

---

## 一、测试环境准备

### 1.1 依赖组件

| 组件 | 要求版本 | 用途 |
|------|----------|------|
| **MQTT Broker** | Mosquitto 2.0+ | 消息代理 |
| **Python** | 3.8+ | 模拟数据生成 |
| **Go** | 1.21+ | 编译运行 |

### 1.2 环境检查命令

```bash
# 检查 MQTT Broker 是否运行
mosquitto_sub -h localhost -t "$SYS/#"

# 检查 Python 版本
python --version

# 检查 Go 版本
go version
```

### 1.3 配置文件验证

确保 `config.json` 配置正确：

```json
{
  "mqtt_broker": "tcp://localhost:1883",
  "mqtt_topic": "edgex/events/core/#",
  "http_port": "8081",
  "max_memory_mb": 64,
  "db_scenario": "extreme"
}
```

---

## 二、测试步骤

### 2.1 启动 MQTT Broker

```bash
# Windows
"C:\Program Files\Mosquitto\mosquitto.exe"

# Linux/macOS
mosquitto -v
```

**验证**：检查端口 1883 是否监听

```bash
netstat -an | findstr ":1883"  # Windows
ss -tlnp | grep 1883           # Linux
```

### 2.2 编译 sfsEdgeStore

```bash
cd d:\MyGo\src\sfsEdgeStore
go build -o sfsEdgeStore.exe .
```

### 2.3 启动 sfsEdgeStore 服务器

```bash
./sfsEdgeStore.exe
```

**预期启动日志**：
```
✓ EdgeX MQTT broker detected at localhost:1883
✓ Subscribed to topic: edgex/events/core/#
✓ Subscribed to topic: edgex/events/#
✓ Subscribed to topic: devices/+/data
✓ sfsEdgeStore started successfully
```

### 2.4 启动设备模拟器

```bash
python test_mqtt_publisher_100devices.py
```

**预期输出**：
```
============================================================
  sfsEdgeStore Production-Ready MQTT Publisher
============================================================
  MQTT Broker: localhost:1883
  Devices:     100
  Anomaly rate: 5%
  Invalid rate: 2%
============================================================
All 100 devices started successfully!
```

---

## 三、测试用例

### 3.1 基础功能测试

| 测试项 | 步骤 | 预期结果 |
|--------|------|----------|
| **MQTT 连接** | 检查服务器日志 | `MQTT broker connected` |
| **主题订阅** | 检查服务器日志 | 订阅 3 个 EdgeX 标准主题 |
| **HTTP 服务** | 访问 http://localhost:8081 | 返回 200 OK |
| **WebSocket 连接** | 打开 Web 界面 | 显示实时数据 |

### 3.2 数据处理测试

| 测试项 | 步骤 | 预期结果 |
|--------|------|----------|
| **消息接收** | 运行模拟器 1 分钟 | `Messages Received > 0` |
| **数据存储** | 查看数据库目录 | 生成 LevelDB 文件 |
| **实时推送** | 查看 Web 仪表盘 | 实时显示设备数据 |
| **异常处理** | 检查日志 | 正确过滤无效数据 |

### 3.3 资源监控测试

| 测试项 | 阈值 | 预期结果 |
|--------|------|----------|
| **内存使用** | RSS < 64 MB | 稳定运行不超限 |
| **CPU 使用** | < 30% | 正常范围内 |
| **Goroutines** | < 100 | 并发控制正常 |

### 3.4 EdgeX 标准兼容性测试

| 测试项 | 标准 | 验证方法 |
|--------|------|----------|
| **主题格式** | `edgex/events/core/device/{name}` | 模拟器发送验证 |
| **消息格式** | MessageEnvelope v2 | 解析无错误 |
| **事件结构** | EdgeXEvent | 正确提取设备信息 |

---

## 四、验证命令

### 4.1 检查服务器状态

```bash
# 查看系统状态（PowerShell）
Invoke-RestMethod http://localhost:8081/api/v1/status | ConvertTo-Json

# Linux/macOS
curl -s http://localhost:8081/api/v1/status | jq .
```

### 4.2 检查 MQTT 消息

```bash
# 订阅所有 EdgeX 主题
mosquitto_sub -h localhost -t "edgex/events/#" -v
```

### 4.3 检查资源使用

```bash
# Windows 任务管理器
tasklist | findstr sfsEdgeStore

# Linux
ps aux | grep sfsEdgeStore
```

---

## 五、测试停止步骤

### 5.1 停止模拟器

```bash
# Windows
taskkill /f /im python.exe

# Linux/macOS
pkill -f "test_mqtt_publisher"
```

### 5.2 停止 sfsEdgeStore

```bash
# Windows
taskkill /f /im sfsEdgeStore.exe

# Linux/macOS
pkill sfsEdgeStore
```

### 5.3 停止 MQTT Broker

```bash
# Windows
taskkill /f /im mosquitto.exe

# Linux/macOS
pkill mosquitto
```

---

## 六、故障排查

### 6.1 常见问题

| 问题 | 原因 | 解决方案 |
|------|------|----------|
| **MQTT 连接失败** | Broker 未启动 | 启动 Mosquitto |
| **无消息接收** | 主题不匹配 | 检查 `mqtt_topic` 配置 |
| **内存超限** | 数据量过大 | 调整 `max_memory_mb` |
| **WebSocket 断开** | 服务未启动 | 检查 HTTP 端口 |

### 6.2 日志分析

关键日志关键字：
- `Subscribed to topic` - 订阅成功
- `MQTT broker connected` - 连接成功
- `WebSocket client connected` - 客户端连接
- `partial_write` - 数据库写入警告

---

## 七、测试报告模板

```
========================================
  sfsEdgeStore 测试报告
========================================
测试日期: 2026-05-11
测试时长: 60分钟
设备数量: 100

【测试结果】
✓ MQTT 连接: 成功
✓ 消息接收: 5,234 条
✓ 数据存储: 5,189 条
✓ 实时推送: 正常
✓ 资源使用: RSS 58MB, CPU 2.3%

【问题记录】
1. 无

【建议】
1. 持续运行 24 小时验证稳定性
========================================
```

---

## 八、版本历史

| 版本 | 日期 | 变更说明 |
|------|------|----------|
| v1.0 | 2026-05-01 | 初始版本 |
| v2.0 | 2026-05-11 | 结构化重构，增加测试用例 |

---

*文档结束*